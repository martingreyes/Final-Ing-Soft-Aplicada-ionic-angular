import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'final-ing-soft-aplicada-pwa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
